__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
*.egg-info/
build/
dist/

# Local library data (normally lives outside the repo, but guard against a
# stray HERMIT_DATA_DIR=. pointing the database in-tree - the VS Code
# "scratch library" run config does exactly that on purpose)
.scratch-library/
*.db
*.sqlite
*.sqlite3

# Books themselves are never part of the repo
*.pdf
*.epub
